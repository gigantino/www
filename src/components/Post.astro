---
interface Props {
  href?: string;
  title?: string;
  description?: string;
}

const { title, description, href } = Astro.props;
---

<a
  class="w-full bg-zinc-50 text-gray-900 dark:bg-zinc-800 rounded border dark:border-zinc-700 dark:text-white px-3 py-4"
  href={href}
>
  <div class="flex flex-col">
    <p class="font-bold">
      {title}
    </p>
    <p class="text-zinc-600 dark:text-zinc-300">
      {description}
    </p>
  </div>
</a>
