---
import Navigation from "@components/Navigation.astro";
import Links from "@components/Links.astro";
import "../styles/global.css";

const { title, description } = Astro.props.frontmatter || Astro.props;
---

<!doctype html>
<html lang="en" transition:animate="none">
  <head>
    <meta charset="UTF-8" />
    <meta property="og:site_name" content="legacy.ggtn.ch" />
    <meta property="og:url" content="https://legacy.ggtn.ch/" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta name="description" content={description} />
    <meta property="og:locale" content="en_US" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="generator" content={Astro.generator} />
    <title>{title + " - ggtn.ch"}</title>
  </head>
  <body class="bg-background text-foreground">
    <div class="mx-auto max-w-3xl px-6 py-14">
      <Navigation />
      <Links />
      {
        Astro.url.pathname != "/" && (
          <div class="mb-8">
            <a href="/">
              <button class="h-8 rounded-md border border-border bg-muted px-3 text-sm font-medium hover:bg-accent">
                {"← Homepage"}
              </button>
            </a>
          </div>
        )
      }
      <main class="prose prose-neutral prose-sm" style={Astro.url.pathname.includes('/blog/') ? "font-family: system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;" : ""}>
        <slot />
        {
          Astro.url.pathname != "/" && (
            <div class="mt-8">
              <a href="/">
                <button class="h-8 rounded-md border border-border bg-muted px-3 text-sm font-medium hover:bg-accent">
                  {"← Homepage"}
                </button>
              </a>
            </div>
          )
        }
      </main>
    </div>
  </body>
</html>
